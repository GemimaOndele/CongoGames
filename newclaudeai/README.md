# newclaudeai — Pack « télécharge puis organise »

- **`QUICK_START_DOWNLOAD.md`** — liens + noms de fichiers + étapes.
- **`download-and-organize-audio-video.bat`** — copie depuis `C:\Downloads\CongoGames` (par défaut) vers `UnityProject/Assets/...`.

La référence unique pour les chemins et licences reste **`docs/AUDIO_VIDEO_DOWNLOAD_GUIDE.md`**.  
Le script de contrôle des doublons : **`tools/Verify-AudioFiles.ps1`** (à la racine du dépôt).

L’ancien dossier **`claudeai/`** redirige vers `docs/` ; **`newclaudeai/`** est la variante « quick start + batch » alignée sur le code actuel (`battle_theme`, `blind_test_theme`, etc.).
