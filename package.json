{
  "name": "congogames",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start-all": "npm --prefix Backend run dev",
    "start-all:prod": "npm --prefix Backend run start",
    "test:cloud": "node scripts/test-cloud.mjs"
  }
}
